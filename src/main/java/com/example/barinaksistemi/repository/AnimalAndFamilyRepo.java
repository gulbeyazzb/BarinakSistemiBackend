package com.example.barinaksistemi.repository;

import com.example.barinaksistemi.entity.AnimalAndFamily;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AnimalAndFamilyRepo extends JpaRepository<AnimalAndFamily,Long> {
}
